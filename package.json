{
  "name": "dps-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:web": "cd web && npm run dev",
    "dev:mobile": "cd mobile && npm run start",
    "build:web": "cd web && npm run build"
  },
  "description": "DPS Monitor — centralized dashboard for tax monitoring via DPS API"
}
